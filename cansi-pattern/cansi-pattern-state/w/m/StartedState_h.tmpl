\#ifndef ${state.start.name.upper()}_STATE_H
\#define ${state.start.name.upper()}_STATE_H

\#include "${state.name.capitalize()}State.h"

void ${state.transitionTo.name}${state.start.name.capitalize()} (${state.name.capitalize()}StatePtr state);

\#endif
