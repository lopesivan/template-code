#ifndef ESTADO_STATE_H
#define ESTADO_STATE_H

/* An incomplete type for the state representation itself. */
typedef struct EstadoState* EstadoStatePtr;

void defaultImplementation (EstadoStatePtr state);

#endif
